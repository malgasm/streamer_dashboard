import { coloredText } from 'soundboard-ui/helpers/colored-text'
import { module, test } from 'qunit'

module 'Integration | Helper | coloredText'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = coloredText 42
  assert.ok result
